import React, { useState } from 'react';

const NotePadHeader = () => {
    return (
        <header className="bg-red-600 h-16 flex items-center justify-center shadow-md">
        <h1 className="text-2xl font-bold text-white">
            Notepad
        </h1>
    </header>
    );
};

export default NotePadHeader;